# Copiá este archivo a ".env" para activar el bonus de IA (reformular respuestas).
# Si lo dejás vacío o no creás el .env, el chatbot funciona igual con las respuestas
# calculadas directamente por Pandas.

LLM_PROVIDER=groq
LLM_API_KEY=
# LLM_MODEL=llama-3.1-8b-instant
